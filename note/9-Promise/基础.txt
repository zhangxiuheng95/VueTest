用于异步计算
可以将异步操作队列化，按照期望的顺序执行，返回符合预期的结果
可以在对象之间传递和操作promise，帮助处理队列

解决异步回调
    之前异步处理通过纯粹的回调函数的形式进行处理，很容易陷入回调地狱-嵌套层次深，剥夺了return的能力

promise
    是一个对象，对象和函数的区别就是对象可以保存状态，函数不行（闭包除外）
    并未剥夺return的能力，因此无需层层传递callback，进行回调获取数据
    代码风格容易理解，便于维护
    多个异步等待合并便于解决

